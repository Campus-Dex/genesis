// Mutable scroll targets written by ScrollTrigger, read every frame by the scene.
// Kept as a plain object so writes never trigger React renders.
export const sceneState = {
  booted: false,
  open: 0, // transmission: shell fragments apart
  recede: 0, // events + countdown: core drifts back into the fog
  finale: 0, // register: core returns, camera pulls out
}
